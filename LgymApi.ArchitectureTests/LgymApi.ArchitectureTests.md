# LgymApi.ArchitectureTests.csproj

- Purpose: Roslyn-based architecture guard tests.
- Contains: dependency direction checks, ID boundary checks, DI placement checks, feature layout checks, mapping checks, enum guards, UoW guards, persistence ownership guards, single-production-DbContext guards, and ownership documentation parsing guards.
- Rules: treat failures as architecture violations unless intentionally documented.
- Task 3 manifests close the Platform-assembly BuildingBlocks and Technical Platform surfaces; semantic fixtures reject unlisted service, repository, DTO, feature command, provider, Contracts, and Models types, including retained Application duplicate sources.
- Boundary: keep tests focused on structural rules, not application behavior.
- `PersistedEntityOwnershipCatalog.cs` is the executable source of truth for the 48 persisted entities and eight owner totals. `PersistedEntityOwnershipDocumentationTests` parses both LF and CRLF Markdown, verifies the view has every catalog row exactly once, and rejects missing, duplicate, and unknown-row fixtures.
- `ModulePersistenceBridgeGuardTests` locks the four internal module persistence seams, exact IVT lists, explicit nine-phase registrar order, same-scoped `AppDbContext` aliases, and negative fixtures for missing or duplicate phases/registrars, foreign sets, extra IVT, and module save access. `SingleProductionDbContextGuardTests` locks the 48-row ownership/DbSet contract, sole context, design-time factory, snapshot, migration root, Npgsql default-schema model, and unchanged solution/project/migration topology; it rejects a second instance of each topology root.
- Module-boundary guards assert semantic zero directly. Omitted or wildcard assembly inputs, relocated sources, path exclusions, and helper-folder classifier evasions are forbidden.
- Issue-380 guards enforce that Application references neither Worker project, that Common retains only its bounded job and email surface, and that Application contracts expose no Worker or Hangfire runtime types.
- `ApiDockerProjectClosureGuardTests` recursively derives the transitive API project closure and locks restore coverage in both `Dockerfile` and `LgymApi.Api/Dockerfile`, the root effective `.dockerignore` context, and alternate explicit post-restore source coverage; run `dotnet test LgymApi.ArchitectureTests/LgymApi.ArchitectureTests.csproj --configuration Release --no-build --filter "FullyQualifiedName~ApiDockerProjectClosureGuardTests"` before the full `dotnet test LgymApi.ArchitectureTests/LgymApi.ArchitectureTests.csproj --configuration Release --no-build`, while the authoritative 18-project, 90-edge graph remains unchanged.
- `ProjectReferenceGraphGuardTests` locks the exact 18-project, 90-edge manifest and documentation. `ProjectReferenceImportGuardTests` compiles every solution source tree with Roslyn, requires 89 direct symbol/import edges plus the one Resources analyzer edge, rejects unused, omitted, transitive, forbidden, duplicate, and cyclic fixtures, and locks the deterministic dependency-first order. `ProjectReferenceDocumentationEvidenceTests` parses all 90 evidence rows, resolves every semantic locator against Roslyn scanner output, validates the Resources analyzer locator against the configured analyzer reference, rejects stale/wrong/duplicate/malformed/added rows, and keeps both issue-375 documents byte-identical to `HEAD`.
- The physical-topology worktree guard ignores only the known IntegrationTests SignalR-client package addition. The graph guards continue to detect any project-reference mutation, including one in that test project, and physical persistence topology changes remain rejected.
- `PlatformReferenceDataBoundaryDocumentationTests` parses the 19-row concern-owner matrix and verifies its source locators, representative registrations, eight canonical owners, 48 persisted entities and DbSets, one `AppDbContext`, and the 18-project, 90-edge graph. It rejects a ninth Reference Data module, provider placement under BuildingBlocks, and second-context claims.
- Issue-381 guards parse the stable Notifications documentation rows and protect provider-neutral contracts, Notifications persisted ownership, Worker/Common runtime seams, provider/runtime leak rules, and compatibility invariants after the approved source move. The source is [`issue-381-notifications-boundary.md`](../docs/modular-monolith/issue-381-notifications-boundary.md).
- #381 guard coverage preserves endpoints, job identities, physical entity locations, the single `AppDbContext`, and the single migration stream while requiring Notifications-owned provider implementation.
- Composition guards verify the closed 15-command and 16-handler registry, Worker testing and non-testing scheduler selection, Notifications FCM ownership, canonical command-ID compatibility, typed persisted job arguments, and unchanged Hangfire job identities.
- `PersistedCommandAuthorizationCatalogTests` locks the 15 canonical persisted-command IDs, their committed-system-intent classification, owner, subject/recipient metadata, registry membership, and declared producer schedule sites without adding an ArchitectureTests-to-Worker reference.
- The API middleware guard pins the exact pipeline from request localization through API idempotency, requires rate limiting exactly once under the negated Testing-environment condition, and rejects swapped, unconditional, inverted, and wrong-environment fixtures.
- Milestone 1 guards also preserve the current-authority boundary, owner-scoped Plan and Exercise behavior, signed local-photo capability semantics, complete semantic route evidence, API-local SignalR session pairing, and committed-system-intent classification for the closed command registry.
- Cancellation propagation guards allow only the exact repository reload and UoW saves inside `PushNotificationDeliveryService.RecoverFromPostClaimFailureAsync` to use `CancellationToken.None`; all other public async service persistence calls must propagate their caller token.
- #391 guards keep Workout & Progress repositories and EF configurations module-owned, require its explicit public read/write contract surface for foreign modules, verify contract DI registrations are unique and owner-scoped, and preserve no-tracking/projection shapes for ranking and progress reads. #386 guards reject Reporting direct measurement repository, entity, writer, and consumer dependencies while allowing only the Reporting-owned accepted-progress command staging path and the Workout & Progress consumer registration.
- Workout & Progress ownership checks also require its seven feature-error source files under `Application/WorkoutProgress/Errors`, reject their legacy `Common/Errors` paths, and preserve the existing canonical module classification.
- Workout & Progress ELO strategy ownership requires the four calculator files under `Application/WorkoutProgress/Scoring/Elo`; classifier fixtures reject Common and BuildingBlocks placements.
- Run the project suite with `dotnet test LgymApi.ArchitectureTests/LgymApi.ArchitectureTests.csproj --configuration Release --no-build`. The existing Todo 20 boundary matrix covered the Application Worker dependency and Common surface guards.
- Issue-389 PlanDay authorization guards verify the implemented consumer-owned Training Planning port uses typed user IDs and its Coaching adapter remains the sole relationship bridge. Target Roslyn fixtures reject reintroduced direct Coaching relationship-service or repository dependencies; Task 20 guards separately lock the retired PlanDay-to-Workout debt at zero.
- Issue-389 Measurements authorization guards verify the implemented consumer-owned Workout & Progress port uses typed user IDs and its Coaching adapter remains the sole relationship bridge. Target Roslyn fixtures reject reintroduced direct Coaching relationship-service or repository dependencies.
- Issue-389 Nutrition authorization guards require Diet Plans and Supplementation to consume only `ICoachingRelationshipAccessService`; their migrated services may not regain direct relationship or role repositories, Coaching persistence, or private Coaching implementations.
- Reporting authorization guards require Reporting and Recurring Reporting dependency bags, production services, and focused test factories to consume only the consumer-owned `IReportingRelationshipAccessPersistence` port; direct Coaching contracts, relationship repositories, and other Coaching internals are rejected.
- Issue-389 Coaching guards require focused public contracts, owner-scoped registrations, consumer-owned relationship-access ports, and isolated Coaching persistence adapters. They reject restored broad Coaching repository or service seams and forbidden foreign persistence dependencies.
- Managed-plan slice guards scan the nonempty extracted `LgymApi.TrainingPlanning` production tree, require its seven managed-plan use cases to use `TrainingPlanning.Errors.PlanErrors`, and prevent every Training Planning source from importing Coaching.
- Issue-390 Nutrition Supplementation guards require the exact nine public one-method contracts, internal implementations, recursively approved command/query types, cancellation parameters, and one registration per contract.
- Issue-393 classifier fixtures require `Nutrition/Errors/SupplementationErrors.cs` to resolve to Nutrition, reject BuildingBlocks and Reporting placements, and reject a retained Common source.
- Reporting error classifier fixtures require `Reporting/Errors/ReportingErrors.cs` to resolve to Reporting, reject BuildingBlocks and Technical Platform placements, and reject a retained Common source.
- Reference Data classifier fixtures require `Platform/ReferenceData/{AppConfig,Enums,Units}`, the legacy-path AppConfig repository port, and `Errors/{AppConfigErrors,EnumErrors}.cs` to retain the Platform / Reference Data owner and reject legacy roots or retained Common sources. The sole public Platform facade composes the internal Reference Data registration helper; Reference Data is not a ninth canonical module.
- Resource and enum guards require English and Polish enum keys using the `EnumType_EnumMember` convention, retain translated lookup `name` and `displayName` alongside a raw `id`, and reject a Domain-to-Resources project reference.
- `ModuleContributionDocumentationTests` keeps a private, focused parser for `docs/MODULE_CONTRIBUTION_GUIDE.md`, rather than generic tooling. It validates stable `module-guide.authority.`, `module-guide.policy.`, `module-guide.path.`, and `module-guide.exception.` matrices, repository-relative `path#Type.Member` locators, semantic source flow and order, and the PR range `<!-- module-contribution-checklist:start -->` through `<!-- module-contribution-checklist:end -->`. Negative fixtures reject invalid rows, locators, ownership and persistence topology, direct Reporting writes, Worker guidance, and malformed PR items. Run its targeted class, then the full ArchitectureTests suite, with the documented Release test commands.
- Issue-390 guards cover all 18 Nutrition D1-D9/S1-S9 contracts and internal implementations, four controller adapters, module-local persistence ports/adapters, one-save/no-transaction rules, legacy-facade absence, and Nutrition's sole allowed Coaching access edge. `NutritionBoundaryDocumentationTests` parses the stable six-owner, 18-action, four-adapter, and `AppDbContext=1`/database=`1`/migration stream=`1`/physical split=`None` documentation rows.
- Module-boundary debt is enforced as direct semantic zero. Dependency, direct entity/repository, and public-surface guards each compile the exact Application, Domain, Platform, Identity, TrainingPlanning, and Notifications production trees, require a nonzero source count for every assembly, and print every observed violation on failure. Negative fixtures reject all five former source-target debt groups, relocated and helper-named sources, nested generic entities while accepting marker IDs, static and non-static broad facades even under `Contracts`, wildcard assembly inputs, and an omitted assembly. Public contract records, interfaces, marker references, action commands, JSON converters, and model classes remain accepted. Run the complete suite with `dotnet test LgymApi.ArchitectureTests/LgymApi.ArchitectureTests.csproj --configuration Release --no-build`.
- Task 33 guard records enforce the reconciled direct `.csproj` graph without changing DataSeeder behavior or the root Project Purpose Map.
- The Platform sub-boundary DAG must have zero production violations; the AppConfig-specific legacy-path repository port is locked as Reference Data through classifier, public-surface, and production-DAG regression coverage without creating a debt exception.
- API handoff guards semantically reject `User`/`Role` entities, entity-typed account or role IDs, Identity repositories, `Items["User"]`, and legacy current-user or route-helper calls in middleware and controllers. Composition requires `AddApplicationApiAdapters` exactly once after Infrastructure and before Notifications API adapters, rejects the transitional facade and wrong order fixtures, and the runtime ledger requires the exact 25 scoped Application plus 3 scoped Notifications adapter contracts. The issue-395 ledger keeps the five pure Workout & Progress wrappers absent and its two owner-oriented input-model adapters present; it also requires all 15 former Todo 13 source paths and old metadata families absent, all 17 owner sources present, all 12 owner adapter contracts and internal implementations compiled, and all 56 relocated/facade exports present.
- The closed module public-surface manifest contains 771 direct Roslyn-observed entries. Manifest parsing rejects duplicate rows before set construction, validates raw cardinality, and then requires exact observed/manifest set equality without relocation transforms or appended entries.
- Owner API-adapter dependency matching is exact by source and target: Identity's account-ELO adapter may consume only `IEloRegistryService`, and AppConfig's adapter may consume only the `AccountReference` marker. Neighboring repositories, owner services, and wrong source paths remain violations; direct entity/repository guards continue to scan the adapter sources.
- Reporting persistence guards lock exactly five marker-safe Application ports and their Infrastructure adapters, stage-only UoW behavior, no-tracking reads, exact DI registrations, and continued absence of the retired broad repositories and upload-init tracker.
- Workout & Progress persistence guards lock exactly seven marker-safe Application ports and Infrastructure adapters, exact DI registrations, stage-only behavior, centralized account-ID conversion, and negative fixtures for Identity entity/repository and commit/transaction leaks.
- Task 20 dependency and entity-leakage guards compile every non-build `LgymApi.TrainingPlanning` source plus the retained Plan and ManagedPlan Application compatibility adapters. They preserve entity-typed persisted keys and EF relationship configuration while rejecting public or direct `User`, `Id<User>`, role repository, Exercise/Training entity or repository, nested-generic, relocated-module, and legacy `Application/PlanDay` fixtures.
- Issue-395 service-dependency guards semantically reject constructor dependency aggregates and service location in Application, Identity, and Notifications business `Service`/`UseCase` classes, including regular, primary, and partial constructors. High-arity direct injection has no numeric cap; DI factory delegates remain valid only when their containing semantic type is composition code. The transaction guard has no exception hook and requires every detected multi-write service method to contain a save or transaction boundary.
- `PartialServiceContributionGuardTests` locks the exact live contributors for Reporting, recurring Reporting, Workout Progress, Exercise, Training, and Worker orchestration. It rejects empty, unreferenced, and unlisted partials; `Issue395MigrationLedgerTests` also keeps the retired legacy password factory absent while the scoped password service remains resolvable.
- `IndependentExportedSurfaceTests` reads `GetExportedTypes()` directly from the five built module assemblies, writes a deterministic observed JSON inventory under `TestResults/Issue395`, validates reviewed per-assembly SHA-256/count approvals, locks the cutover's required/removed identities, and uses compiled fixtures to reject unlisted and public provider exports independently of the source manifest.
- `Issue395FinalDocumentationTests` parses ADR-007 and issue-395 final verification. It resolves documented source locators, requires the 25 plus 3 API-adapter ownership record, the three retained Notifications integration adapters, 18 projects, 90 direct edges, 48 entities, 46 mapping profiles, 771 direct source entries, one deployable and one physical persistence topology, two Mermaid diagrams, and Todo 22 placeholders. Fixtures reject stale locators, incorrect owner/count/context claims, a filled evidence placeholder, and historical issue-375 mutation.
- `SonarSuppressionGuardTests` parses source declarations to lock exactly nine justified class-local `S3453` marker suppressions and one justified method-local test-fixture `S5344` suppression. It rejects extra targets, wrong categories or rule IDs, and assembly/module/global suppression while preserving private marker constructors and legacy PBKDF2 fixture values.
- `RowSecurityProviderIsolationGuardTests` allow RLS identifiers and Npgsql connection/command/transaction types only in the actor scope, startup runtime validators, and migrations; they require parameterized transaction-local actor setup with direct disposal on failure.
